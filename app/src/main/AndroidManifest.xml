<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mypethealth.com.myapplication" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".temperatureGraph"
            android:label="@string/title_activity_temperature_graph"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>
        <activity android:name=".Air_Quality_graph" >
        </activity>
        <activity
            android:name=".AirQualityGraph"
            android:label="@string/title_activity_air_quality_graph"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="mypethealth.com.myapplication.MainActivity" />
        </activity>
        <activity
            android:name=".ActivePoints"
            android:label="@string/title_activity_active_points"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>
    </application>

</manifest>
